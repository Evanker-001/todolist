server:
  port: 9090

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/users?serverTimezone=GMT%2b8
    username: root
    password: abc951

mybatis:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
